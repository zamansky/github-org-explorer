query LoadOrgs($username: String!){ 
  user(login:$username){
    organizations(first:2){
      pageInfo{
        hasNextPage
      }
      edges{
        cursor
        node {
          name

        }
      }
    }
  }
}

